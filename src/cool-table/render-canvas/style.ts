import styled from "styled-components";
export const boxHeight = 800;
export const boxWidth = 700;

export const Box = styled.div`
    border: 1px solid black;
    width: ${boxWidth}px;
    height: ${boxHeight}px;
`;
